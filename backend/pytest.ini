[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    # Aviso de Starlette sobre el cliente de tests; no afecta a los resultados.
    ignore:Using `httpx` with `starlette.testclient`:DeprecationWarning
